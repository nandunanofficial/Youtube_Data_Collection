name: YouTube Trending Keyword Extraction

on:
  schedule:
    - cron: '0 * * * *'  # Runs every hour
  workflow_dispatch:  # Allows manual execution

jobs:
  fetch-trending-data:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: Set Up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install Dependencies
        run: pip install pandas requests

      - name: Fetch YouTube Trending Data
        run: python fetch_trending.py

      - name: Upload Trending Data
        uses: actions/upload-artifact@v4
        with:
          name: youtube_trending_data
          path: youtube_trending_data.csv

  extract-keywords:
    runs-on: ubuntu-latest
    needs: fetch-trending-data  # Ensure trending data is fetched first
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: Set Up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install Dependencies
        run: pip install pandas nltk wordcloud matplotlib

      - name: Download NLTK Data
        run: python -c "import nltk; nltk.download('punkt')"

      - name: Download Trending Data
        uses: actions/download-artifact@v4
        with:
          name: youtube_trending_data
          path: .

      - name: Run Keyword Extraction Script
        run: python extract_keywords.py

      - name: Upload Word Cloud
        uses: actions/upload-artifact@v4
        with:
          name: wordcloud
          path: wordcloud.png
